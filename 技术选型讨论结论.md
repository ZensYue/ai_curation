# 技术选型讨论结论

## 1. 项目定位

当前项目定位不再是单一的内部自动化工具，而是一个平台化产品，后续将包括：

- Web 端页面
- 用户登录与权限管理
- 产品录入与任务发起
- 多平台比价与分析
- AI 润色与分析能力
- 税率、利润等业务计算
- 飞书、钉钉等通知能力
- 后续营销、拉新等平台功能

在这个定位下，爬虫是平台中的一个核心能力服务，但不是整个平台本身。

## 2. 总体技术方向

已收束出的总体方案如下：

- 前端使用 `Vue3`
- 主平台后端使用 `Java`
- 不采用 `RuoYi` 作为强绑定底座，改为自建轻量平台底座
- 爬虫能力使用 `Python`，并优先作为独立服务建设
- 服务之间首期采用 `HTTP + 回调` 的异步协作方式
- 轮询不作为主链路，而作为补偿和兜底机制
- 后续通知能力统一抽象，接入飞书、钉钉机器人

## 3. 为什么主平台选择 Java

平台主系统承载的是中后台和产品化能力，重点不在网页抓取，而在以下能力：

- 账号体系
- 权限控制
- 客户与业务管理
- 任务流转
- 审计与日志
- 后续营销与运营能力

这部分更适合使用 Java 进行长期建设，原因是：

- 更适合作为稳定的平台主干
- 对多人协作和长期维护更友好
- 更利于后续模块扩展和服务治理
- 在权限、管理后台、业务系统建模方面更成熟

因此，Java 作为主平台语言是当前阶段更合适的选择。

## 4. 为什么爬虫服务选择 Python

虽然整体平台主干使用 Java，但爬虫服务单独选择 Python 更合适。

原因主要有：

- 多站点适配开发效率更高
- 浏览器自动化生态更成熟
- HTML 解析、数据清洗、文本处理更顺手
- 更适合快速应对页面结构变化和反爬策略变化
- 后续接 AI 做标题清洗、型号标准化、语义处理更自然

因此结论是：

- 平台主干：`Java`
- 爬虫能力域：`Python`

这不是重复建设，而是按职责做合理分工。

## 5. 底座选择结论

关于是否使用 `RuoYi`，当前讨论后的结论是：

- `RuoYi` 不是必需品
- 不建议将整个平台架构深度绑定在 `RuoYi` 上
- 更适合采用自建轻量平台底座

这样选择的原因是：

- 当前项目不是传统通用后台系统，而是会持续演化的平台产品
- 核心竞争力在任务编排、平台适配、AI 分析、比价与利润逻辑，不在后台模板
- 若深度绑定后台脚手架，后续容易受到代码风格和架构边界限制

因此，最终方向为：

- 使用 `Spring Boot` 体系自建底座
- 自行建设认证、权限、任务、通知、业务模块
- 保持平台架构边界清晰，避免被通用后台模板反向塑形

## 6. 爬虫服务形态结论

爬虫服务首期作为一个独立服务建设，但暂时不拆成多个微服务。

当前达成的共识是：

- 先做一个独立的 Python 爬虫服务
- 不按平台拆成多个独立部署服务
- 但代码内部要按平台适配器方式组织

也就是说：

- 每个平台单独一个 adapter
- adapter 是模块边界，不是部署边界
- 服务内需要具备任务分发、平台识别、执行、标准化等能力

这样做的好处是：

- 首期复杂度可控
- 部署与运维成本低
- 后续若某个平台特别复杂，再按需要拆分

## 7. 服务协作方式结论

关于 Java 平台与 Python 爬虫服务之间如何协作，当前结论是：

- 主链路采用 `HTTP + 回调`
- 轮询仅作为补偿和异常兜底

原因如下：

### 回调适合作为主链路

- 异步任务完成后可以立即通知平台
- 实时性更好
- 平台端不需要持续高频查询
- 更符合爬虫任务执行时间不稳定的特点

### 轮询适合作为补偿链路

- 当回调失败或丢失时，可以主动补查
- 适合处理长时间未返回、状态异常等情况
- 可作为任务巡检机制存在

因此，最终结论不是“二选一”，而是：

- `回调优先`
- `轮询兜底`

## 8. 通知能力结论

飞书、钉钉等能力后续会接入，但不建议直接散落在业务代码中。

当前建议是：

- 将通知能力抽象成统一模块
- 后续统一接入飞书、钉钉机器人
- 业务服务只关心“触发什么通知”
- 通知模块负责“发到哪个渠道、用什么模板、发给谁”

这样做的好处是：

- 后续扩渠道更容易
- 不会让业务服务与具体通知平台强耦合
- 更利于统一告警、业务提醒、客户通知等不同场景

## 9. 当前阶段的推荐总体架构

基于现有讨论，当前推荐的总体架构如下：

- 前端：`Vue3`
- 主平台：`Java + Spring Boot`
- 爬虫服务：`Python`
- 服务协作：`HTTP + 回调`
- 状态补偿：`轮询`
- 通知能力：统一抽象，后续接飞书 / 钉钉

从职责边界看：

- Java 负责平台主业务
- Python 负责采集与适配能力
- 通知能力作为独立公共模块建设

## 10. 当前已确认的关键原则

截至目前，已明确的原则如下：

1. 项目按平台化产品建设，而不是单一自动化脚本工程。
2. 前端使用 `Vue3`。
3. 平台主后端选择 `Java`。
4. 不强依赖 `RuoYi`，采用自建轻量底座。
5. 爬虫能力由 `Python` 独立服务承载。
6. 爬虫服务首期不拆成多个微服务，但内部必须 adapter 化。
7. Java 与 Python 之间首期采用 `HTTP + 回调` 协作。
8. 轮询不作为主方案，而作为补偿机制保留。
9. 飞书、钉钉等通知能力后续统一抽象接入。

## 11. 下一步建议

当前不继续下钻接口和字段细节，建议下一阶段重点收束以下内容：

- 平台主服务的模块边界
- Python 爬虫服务的内部模块划分
- 任务流转的高层时序
- 通知模块的职责边界
- 首期最小可行架构

以上内容可在下一轮讨论中继续细化。
